# Backlog

The live backlog. One row per item, ordered by what stands between the product and being sold safely — not by when it was noticed.

When an item is picked up it gets an execution spec in [`features/`](../features/README.md) and its row links to it. Reasoning behind each block is in [`docs/sot/`](./sot/README.md).

**This file is not where the next task is chosen.** It answers *what is missing and how much it matters*; the [build order](./sot/10-saas-roadmap.md#build-order) answers *what is next*, because it carries the dependencies between items and a priority column cannot. When the two disagree, **the chain wins** — a priority is a judgement and can be revised, a dependency cannot. There was one live case — *shared rate-limit store (Redis)* in P1 needing the Redis that the P2 job queue brings. [`features/0017`](../features/0017-job-queue-for-pdf-embedding.md) brought the Redis and [`features/0018`](../features/0018-shared-rate-limit-store.md) then closed the P1 item, so the inversion is resolved and both rows are gone.

Last reviewed: **2026-09-01** — the priorities were re-read against the code and the shortlist that orders them now lives in [what comes next](./sot/10-saas-roadmap.md#what-comes-next).

## P0 — Blocks any real customer

**Empty.** The data-loss defect, the unthrottled public write paths, the ReDoS surface, the red E2E suite, the unenforced Node version and the publicly served uploaded PDFs are all closed ([`features/0001`](../features/0001-stable-field-ids-and-safe-bulk-save.md)–[`0006`](../features/0006-signed-expiring-urls-for-uploaded-pdfs.md)). Security headers and a CSP ([`features/0007`](../features/0007-security-headers-and-csp.md)) and session hardening ([`features/0008`](../features/0008-session-hardening.md)) are closed too. **There are no High findings left open.** What remains in P1 is privacy and operational work — of it, structured logging (S9) is the one that unblocks the most, because CSP reporting and any answer to "what happened to our submission at 14:32" both wait on it.

## P1 — Security, privacy and operational readiness

| Item | Why | Reference |
|---|---|---|
| Plan limits are read-then-write, so two concurrent requests can both pass at the boundary | `assertCanInvite` reads the seat count and `createInvitation` writes the row in a separate statement, with no transaction and no row lock — so two invitations sent at the same moment on the last seat can both pass the check and both be created, leaving the organization one seat over what was paid for. `assertCanPublishForm` has the identical shape and the same window. **Not introduced by [`features/0015`](../features/0015-team-plan-and-purchased-seats.md)** — it is the pattern every non-metered limit has had since [`features/0012`](../features/0012-plan-catalogue-and-entitlements.md), and 0015 correctly did not change it while wiring the seat check. Found by `saas-readiness-reviewer` on the 0015 branch. The shape of the fix already exists in this codebase: `assertResponseWithinLimit` claims the response meter with an atomic upsert-and-compare inside the submission's transaction precisely because read-compare-increment lets two submissions through at `limit - 1`. The impact here is milder than for responses — one seat or one published form over, by a customer who is paying, not a free-tier bypass — which is why it is P1 and not urgent | [`features/0012`](../features/0012-plan-catalogue-and-entitlements.md) · [04-backend-patterns §10](./sot/04-backend-patterns.md) |
| Account-level lockout on repeated failed logins | Per-IP limiting does not stop credential stuffing distributed across hosts. Deliberately deferred: a per-account limiter without an unlock path lets anyone lock a named user out by spamming their address, so it needs the notification and unlock flow designed with it (S10). [`features/0018`](../features/0018-shared-rate-limit-store.md) sharpened the need for that flow rather than reducing it: counters now survive a restart, so even the per-IP limit has no remedy but waiting out the window or a `redis-cli DEL` ([08-operations](./sot/08-operations.md)) | [07-security](./sot/07-security-and-privacy.md) (S10) |
| No way to lift a rate limit on one identity without `redis-cli` | Counters live in Redis and survive a restart ([`features/0018`](../features/0018-shared-rate-limit-store.md)), which is the fix — and it removes the accidental unlock that redeploying used to be. Clearing one locked-out client is a documented `redis-cli DEL` against a key whose shape [08-operations](./sot/08-operations.md) spells out. That is fine for an operator and is not a support process: it needs shell access to production for something a support agent should be able to do. Small, and it belongs with the unlock flow that account-level lockout needs anyway | [07-security](./sot/07-security-and-privacy.md) · [`features/0018`](../features/0018-shared-rate-limit-store.md) |
| A global fallback rate limiter | Only the three unauthenticated write paths are limited. Everything else — including `GET /api/forms/public/:shareId` and `/uploads` — is unthrottled. Picking a global number that does not break the editor's legitimate bursts needs traffic data we do not have yet | [07-security](./sot/07-security-and-privacy.md) |
| No UI for authoring a field `pattern` | `FieldPropertiesPanel.vue` has no pattern input, so the only way to set one is the API. Now that patterns are validated and a useful `400` names the problem, a UI is worth having — it needs live validation and must explain that lookahead/lookbehind/backreferences are unsupported | [`features/0004`](../features/0004-safe-author-supplied-regex.md) |
| Frontend still compiles patterns with native `RegExp` | `frontend/src/composables/useFormValidation.ts:42` runs the author's pattern in the respondent's browser. Lower severity than the server case — it burns the respondent's own tab, not shared infrastructure, and it is already `try/catch`-wrapped — but it should agree with the backend's engine, or the two will disagree about which patterns are valid | [`features/0004`](../features/0004-safe-author-supplied-regex.md) |
| `Form.pdfUrl` is an unconstrained client-supplied string | `createFormSchema` / `updateFormSchema` accept any `z.string()`, so an author can point their own form at another author's uploaded filename. Filenames are unguessable, so this is obscurity, not a control — and since [`features/0006`](../features/0006-signed-expiring-urls-for-uploaded-pdfs.md) the API will mint a valid signed URL for whatever filename is in the column. The fix is to verify at write time that the file came from an upload this user made, which needs an uploads table | [07-security](./sot/07-security-and-privacy.md) |
| CSP violation reporting | The SPA's policy has no `report-uri`/`report-to`, so a policy the app quietly breaks in someone's browser is invisible. Needs somewhere to send reports and something that reads them, which is the structured-logging item below — do them together. **Its dependency is met**: structured logging shipped in [`features/0025`](../features/0025-structured-logging.md), so there is now somewhere for a report to go and something that can correlate it. | [07-security](./sot/07-security-and-privacy.md#what-the-spa-policy-does-not-cover) · (S9) |
| `style-src` still needs `'unsafe-inline'` | Measured in [`features/0007`](../features/0007-security-headers-and-csp.md): 373 violations from `style` attributes (Vue `:style`, the editor's positioned overlays) and 50 from PrimeVue injecting `<style>` at runtime. Splitting into `style-src-elem`/`style-src-attr` grants the same permission and was tried. Narrowing it means changing how the app styles things — worth doing only alongside a deliberate styling change, not on its own | [07-security](./sot/07-security-and-privacy.md#what-the-spa-policy-does-not-cover) |
| Session listing and per-session revocation | A user cannot see the sessions on their account or end one from another device, and there is no idle timeout — a refresh token lives its full 7 days whether used or not. `refresh_tokens` already carries everything needed (`family`, `createdAt`); what is missing is an endpoint and a screen | [07-security](./sot/07-security-and-privacy.md#what-the-session-model-does-not-cover) · [`features/0008`](../features/0008-session-hardening.md) |
| Nothing migrates the integration test database locally | CI runs `prisma migrate deploy` against `vuepdf_test` before the integration suite; no local script does, so the first run after any migration fails with `relation does not exist` and looks like a broken test rather than a missing step. Hit while building [`features/0008`](../features/0008-session-hardening.md). A `pretest:integration` step would close it | [09-quality](./sot/09-quality-and-testing.md) |
| Account deletion and per-account data export | GDPR erasure and portability; neither exists | (S8) |
| Response retention policy and a respondent privacy notice | IP and user agent are collected from respondents with no notice, no limit and no actual anti-abuse use | (S7) |
| Password policy, lockout, breach check | 6-character minimum, unlimited attempts | (S10) |
| ESLint flat config, then lint in CI | `npm run lint` currently succeeds while linting nothing, because no workspace defines a `lint` script and there is no `eslint.config.js`. Type check and build now run in CI; lint is the remaining piece | [09-quality](./sot/09-quality-and-testing.md) |
| Validate all configuration at boot with Zod | Only `JWT_SECRET` is checked; a wrong `BASE_URL` silently produces broken PDF links | [08-operations](./sot/08-operations.md) |
| Production build must generate the Prisma client before pruning devDependencies | `backend`'s `postinstall` runs `prisma generate`, but `prisma` is a devDependency, so `npm ci --omit=dev` would run the hook without its CLI. Harmless today (no deploy pipeline exists), and a trap for whoever writes one | [08-operations](./sot/08-operations.md) |
| Automated backups with a tested restore | None exist; recovery time is unknown | [08-operations](./sot/08-operations.md) |
| Error tracking on API and SPA | Browser-side editor failures are invisible today. **Its dependency is met** ([`features/0025`](../features/0025-structured-logging.md)): every server-side line now carries a request id, so a tracked exception can be tied to the request that caused it. This is item 2 of the observability list in [08-operations](./sot/08-operations.md) and item 1 is done. | [08-operations](./sot/08-operations.md) |

## P2 — SaaS foundations, design and go-to-market

Most of this is a dependency chain — see the build order in [10-saas-roadmap](./sot/10-saas-roadmap.md).

**Two things here are not.** The **landing page** blocks nothing and nothing blocks it; the roadmap keeps it as a [parallel track](./sot/10-saas-roadmap.md#parallel-track-the-landing-page) rather than pretending it has a place in the chain. And the rows about facts and prices are decisions for a person, not work for an engineer — they are here because they block a launch just as hard.

| Item | Why | Reference |
|---|---|---|
| **Express 5 would delete `asyncHandler` and its scan** | Express 5 forwards a rejected promise from an `async` handler to the error handler itself, so it needs no wrapper and no discipline — which is strictly better than [`features/0026`](../features/0026-async-handler.md), whose whole design exists to work around Express 4. The usual blocker does not apply here and was **checked**: `path-to-regexp` v8 removed regex paths, optional `:param?` segments and bare `*` wildcards, and all 41 route paths in this repository are literals or plain `:param`; `req.query` is never assigned and `res.sendFile` is not used. What makes it its own change rather than a line in 0026: it alters every route and every middleware at once — `helmet`, `cors`, `cookie-parser`, `express-rate-limit`, and the raw-body Stripe mount that must stay above `express.json()`. **Whoever does it should delete `middleware/asyncHandler.ts` and `tests/async-handler-coverage.spec.ts` in the same change**, and both say so in their own comments | [04-backend-patterns §1](./sot/04-backend-patterns.md) · [`features/0026`](../features/0026-async-handler.md) |
| **An organization cannot be renamed** | `GET /api/organizations` returns the name and the switcher shows it ([`features/0023`](../features/0023-active-organization.md)), so the missing half is one `PATCH` and a field — plus the decision 0023 deliberately did not take: whether renaming is an owner's act or an admin's. `SettingsView.vue` still says out loud that this is not built. Note the one thing that makes it more than a text input: `Organization.slug` is `@unique` and is meant for URLs and future white-labeling, so whether renaming re-slugs — and what happens to anything already pointing at the old one — is the real question | [`features/0023`](../features/0023-active-organization.md) · [03-domain-model](./sot/03-domain-model.md) |
| Email delivery for invitations, and now for webhook failures too | There is no mail provider at all, so an invitation is a link the inviter copies and sends themselves. It works, and it puts the burden on the customer — and if they lose the link before sending it, the invitation is unusable and must be reissued. Wants the job queue for retries and a delivery record — which [`features/0017`](../features/0017-job-queue-for-pdf-embedding.md) has now built, so this needs a mail provider and a second job type rather than infrastructure | [07-security](./sot/07-security-and-privacy.md#invitations-are-bearer-capabilities) · [`features/0010`](../features/0010-member-invitations-and-role-enforcement.md) |
| No audit of who invited, removed or promoted whom | `Invitation.invitedByUserId` records who sent an invitation and nothing records who removed a member or changed a role. For a B2B buyer that is a due-diligence question, and after an incident it is the only way to answer "who let them in". Wants structured logging (S9) first | [08-operations](./sot/08-operations.md) · (S9) |
| An organization can outlive its last member — **via account deletion only** | [`features/0010`](../features/0010-member-invitations-and-role-enforcement.md) closed the two reachable-from-the-app paths: the last owner can no longer demote or remove themselves. What remains is `Membership.user` cascading on **user deletion**, which would still strand the organization and its forms with nobody able to reach them. Needs a rule decided alongside account deletion (S8), which is the only thing that can trigger it | [03-domain-model](./sot/03-domain-model.md) · [`features/0009`](../features/0009-organizations-own-resources.md) |
| **Decide the landing technology** | Nobody has chosen, and if this stays undecided it gets decided by whoever opens an editor first. It is a real decision because the landing has requirements the SPA does not: it must be indexable, it must be fast on a cold first visit, and it is the one page whose audience has never heard of us. The options, their real costs and a recommendation are in [02-architecture](./sot/02-architecture.md#the-landing-page-an-open-decision-not-implemented). Blocks the landing build and nothing else | |
| **Build the landing page** | The commercial front door, and it does not exist. Designed already — desktop and phone artboards, one offer, one repeated action (*Start free*), and the hook is the differentiator that is already built: a PDF that is already a form keeps its fields on upload. A **parallel track**, not a step in the chain — it blocks nothing and nothing blocks it | [05-frontend-patterns §8](./sot/05-frontend-patterns.md) · [01-product-and-market](./sot/01-product-and-market.md) |
| Facts the landing cannot ship without | The canvas leaves eight things bracketed on purpose, because they are decisions rather than design: `[SUPPORT EMAIL]`, `[LEGAL ENTITY NAME]`, `[REGISTERED ADDRESS]`, `[CUSTOMER LOGO]`, and the `[NAME]` / `[ROLE]` / `[COMPANY]` / `[CITY]` of one real reference. **The prices on it are also not a decision anyone has taken** — they match the Plan & usage screen and were drawn to make the page whole. None of this is engineering work and all of it blocks launch | |
| The canvas promises role semantics the API does not enforce | The **Members & roles** artboard says a member sees "only the forms they created". `backend/src/routes/forms.ts` scopes forms by organization and checks *membership*, not role, so every member reaches every form and every response in the organization. [`features/0011`](../features/0011-adopt-the-design-system.md) made `MembersView.vue` state what the guards actually do rather than what the canvas says, which is honest but is not the product the design describes. Decide which one is right — per-creator scoping is a real B2B expectation, and shipping the canvas's wording without it would be telling a customer their data is partitioned when it is not | |
| A Stripe account needs a Customer Portal configuration created by hand before `/billing/portal` works | Discovered the hard way on 2026-08-31: a fresh account has **zero** portal configurations, and `billingPortal.sessions.create` fails with `No configuration provided`. Nothing in the repo said so, and no test could catch it — the suites mock that call. The test-mode account now has one (`at_period_end` cancellation, invoice history, card update), created via the API. **It is per-account, so live mode will need its own**, and the settings are a product decision: what a customer may cancel, change and see. Recorded in [08-operations](./sot/08-operations.md); the row stays open until live mode is configured too | [`features/0013`](../features/0013-stripe-subscriptions.md) |
| **Nobody has decided the prices** | Not the Pro amount, not Team's base, and not the per-seat amount. The canvas draws €0 / €12 / €39 + €6 per seat and those are drawings. This blocks nothing technically — **no amount is stored or rendered anywhere in this repository** and none ever should be: the figure lives in Stripe, the application stores price ids (`STRIPE_PRICE_PRO`, `STRIPE_PRICE_TEAM`), and the customer sees the real number on Stripe's own Checkout page. It blocks *selling*. Related and equally undecided: **how many seats Team's base price includes** — [`features/0015`](../features/0015-team-plan-and-purchased-seats.md) shipped `seats: 3` in `backend/src/services/plans.ts` as the floor a subscription degrades to when Stripe reports no quantity, and three is a *safe* number rather than a decided one (the alternative, `null`, means unlimited and would give the product away to any Team subscription whose quantity could not be read). The code works with any value; changing it is one constant | [`features/0013`](../features/0013-stripe-subscriptions.md) · [`features/0015`](../features/0015-team-plan-and-purchased-seats.md) |
| Tax, VAT and invoicing | The canvas says "VAT excluded" and nothing in the product acts on it. Stripe Tax is configuration plus a decision about where the business is registered, so this is blocked on the legal entity (row above) rather than on code. Invoices are already Stripe's — the customer reads them in the Customer Portal — so what is actually missing is the tax treatment, not a document | [`features/0013`](../features/0013-stripe-subscriptions.md) |
| The response allowance can lapse mid-period and take live forms offline | An organization that downgrades mid-month having taken 800 responses is instantly over Free's 50, and **every one of its public forms starts answering `404`** — correctly, since a respondent must never be told about the owner's billing state. [`features/0013`](../features/0013-stripe-subscriptions.md) narrowed how often this can happen (`past_due` keeps the paid plan, and a cancellation lands at the period end), so the only route to it is a payment that finally failed. The remaining cliff is real and is **not** solved: the author gets no warning, and finds out when somebody tells them their form is broken. It needs a way to tell the author something, which is the row below | [`features/0013`](../features/0013-stripe-subscriptions.md) · [07-security](./sot/07-security-and-privacy.md) |
| Per-file revocation for signed PDF URLs | Today revocation is all-or-nothing: rotate `JWT_SECRET` (invalidating every outstanding link) or delete the file. Withdrawing access to one PDF while leaving others valid needs a database-backed nonce or key version per file. Cheap to add alongside the uploads table above | |
| Move PDF **extraction** off the request path, with the async UX it needs | [`features/0017`](../features/0017-job-queue-for-pdf-embedding.md) queued the embed and deliberately left the other two PDF operations inline: `extractFieldsFromPDF` inside `POST /api/upload`, and `syncFieldsFromPDF` inside the first `GET /api/forms/:id`. Both responses **carry** the fields they just extracted — `frontend/src/composables/useFormManagement.ts` hands `response.fields` straight to `formFieldsStore.loadFieldsFromPDF`, and the editor draws them the instant the upload finishes — so making them asynchronous is not a refactor, it is a product change. It needs three things designed together: a "we are still reading your document" state, a way for the client to learn the answer (polling or a socket), and a decision about what happens when extraction fails *after* the author has already started drawing their own fields. Until then a large or pathological PDF still blocks the event loop on upload. The queue itself is built and would need only a second job type | [02-architecture](./sot/02-architecture.md) · [`features/0017`](../features/0017-job-queue-for-pdf-embedding.md) |
| Two replicas embedding one form can still lose an update — **only on the inline path now** | [`features/0016`](../features/0016-object-storage-for-uploaded-pdfs.md) fixed the common case with an **in-process** lock (`services/organization-lock.ts`), which two replicas do not share. [`features/0017`](../features/0017-job-queue-for-pdf-embedding.md) closes it **when `REDIS_URL` is set**: the embed runs in a worker under a Redis lock per form, which is a serialiser that spans processes, and `tests/integration/pdf-embed-queue.spec.ts` asserts that a save made during an in-flight embed still reaches the PDF. The row stays open because **the queue is optional and unset is the default**: a deployment running two replicas with no `REDIS_URL` has exactly the old race. It is narrow — two replicas *and* two saves of one form within the same second — so the answer is a deployment rule rather than more code: **more than one API replica means `REDIS_URL` and a worker**, which [08-operations](./sot/08-operations.md) now says | [04-backend-patterns §5](./sot/04-backend-patterns.md) · [`features/0017`](../features/0017-job-queue-for-pdf-embedding.md) |
| Deleting a form deletes no PDF, and object storage makes that cost money | Nothing in this codebase removes a stored PDF except the invalid-upload path in `routes/upload.ts`. Deleting a form removes its rows and leaves the document, so every deleted form's PDF is retained for ever. On local disk that was unmeasured waste; on the `s3` driver from [`features/0016`](../features/0016-object-storage-for-uploaded-pdfs.md) it is a bill that only grows, and — the part that matters more — it is **customer data retained after the customer deleted it**, which is a GDPR answer this product would rather not have to give. Deliberately not built into 0016: hard rule 5 says writing a delete means answering what customer data it destroys and whether anybody asked for that, and a storage migration is not the change in which to start destroying documents. Needs `storage:migrate`-style care and an answer for the `-backup.pdf` copies the field-embedding migration leaves behind | [07-security-and-privacy](./sot/07-security-and-privacy.md) · [03-domain-model](./sot/03-domain-model.md) |
| **Nothing tells a customer their webhook endpoint was disabled** | After ten consecutive failures [`features/0020`](../features/0020-outbound-webhooks.md) switches an endpoint off, which is right — a dead endpoint is otherwise a queue filling with doomed jobs. But **this product has no email service** ([`features/0010`](../features/0010-member-invitations-and-role-enforcement.md) had to make invitations a link the inviter sends), so the owner finds out by reading `disabledAt` and `lastError` through the API. That is a real gap rather than a rough edge: an integration can stop working for days and nobody is told. Needs the mail provider that invitation delivery also wants — one dependency, two rows. **Narrowed by [`features/0022`](../features/0022-webhooks-screen.md)**: the webhooks tab now shows the disabled state, its `lastError` and its delivery history, and offers a **Re-enable** that keeps the endpoint's id and secret — so a customer who looks can see it and recover. What is still missing is being *told* without looking | [`features/0020`](../features/0020-outbound-webhooks.md) · [08-operations](./sot/08-operations.md) |
| **A webhook endpoint's signing secret cannot be rotated** | `mintWebhookSecret` runs once, at creation ([`features/0020`](../features/0020-outbound-webhooks.md)), and nothing else ever writes `webhook_endpoints.secret`. So a customer who leaks their secret — into a log, a repository, a screenshot — has exactly one remedy: delete the endpoint and create another, which is a **different** secret *and* a different id, and their receiver stops verifying at the moment they press it. What makes this more than an endpoint: rotation done properly needs a window where **both** secrets verify, so the customer can deploy the new one without dropping the events in between — which means a second column and a decision about how long the old one lives. Found while specifying [`features/0022`](../features/0022-webhooks-screen.md), which deliberately does not do it: its `PATCH` re-enables and nothing more | [`features/0020`](../features/0020-outbound-webhooks.md) |
| Replaying a webhook delivery | The delivery log records every attempt, so the missing piece is an endpoint that re-sends one. Note what it cannot do and why: the log holds **no payload body** (that would be a second copy of respondent answers, outliving the form they came from), so a replay re-renders the event from the `Response` — and an event whose response was deleted cannot be replayed at all | [`features/0020`](../features/0020-outbound-webhooks.md) |
| More webhook event types | Only `response.created` exists. `form.published`, `response.deleted` and the rest are each a payload decision rather than machinery: the queue, the signature, the retries and the log are already general. `WebhookEndpoint.events` is a list precisely so that adding one is not a migration | [06-api-reference](./sot/06-api-reference.md) |
| Write endpoints on `/api/v1` | The API is read-only ([`features/0019`](../features/0019-api-keys-and-read-only-public-api.md)): list and read forms, list and export responses. Creating forms, editing fields and deleting responses each need their own thinking about idempotency and about what a script may destroy — hard rule 5 applies with more force to a caller that is a loop. Deliberately deferred rather than forgotten | [06-api-reference](./sot/06-api-reference.md) |
| Per-key scopes, and key expiry | One key reads everything its organization can read, and never expires unless revoked ([`features/0019`](../features/0019-api-keys-and-read-only-public-api.md)). Scopes are worth having once there is something to scope — which is to say, once there are write endpoints — and expiry needs a rotation flow designed with it, or it is just a way to break integrations on a schedule. The `revokedAt` column and the `lastUsedAt` timestamp are already there | [07-security](./sot/07-security-and-privacy.md) | |
| Usage metering: no proration and no record of a mid-month plan change | **The meter exists** — `UsageCounter`, incremented inside the submission's transaction, counting submissions accepted in the period ([`features/0012`](../features/0012-plan-catalogue-and-entitlements.md)) — and **Stripe shipped** ([`features/0013`](../features/0013-stripe-subscriptions.md)), so "close it with Stripe" is no longer the answer. What is still open is what an invoice would have to agree with: an organization that upgrades on the 20th keeps the same `UsageCounter` row it had on Free, so the month's count spans two plans and nothing records where the boundary was. Nothing reconciles the counter against Stripe either. It does not bite while the only paid plan is a flat monthly fee — the count is a limit, not a billed quantity — and it becomes real the moment anything is charged per use | [`features/0013`](../features/0013-stripe-subscriptions.md) · [`features/0012`](../features/0012-plan-catalogue-and-entitlements.md) |

## P3 — Product and technical debt

| Item | Why | Reference |
|---|---|---|
| **An E2E test fails under load** | *offers to save an unsaved document on the way out* (`e2e/form-management.spec.ts`) timed out once waiting 30s for `/dashboard` after **Save and leave**, on a run where the whole suite took 43s instead of the usual 20 — the machine was busy running other suites in parallel. It then passed five times in a row, twice as part of the full suite, and it passes on `develop` too, so it is a timing flake rather than a defect in the flow. Worth knowing because CI machines are loaded by definition: the step it waits on creates a form and uploads a PDF, so the 30-second budget is the thing to revisit if it recurs. Found while running the gate for [`features/0024`](../features/0024-organization-responses.md) | [09-quality](./sot/09-quality-and-testing.md) |
| PDF round-trip test: editor position → embedded AcroForm position | Nothing verifies the canvas-to-PDF scale coupling; it silently breaks on any zoom change | |
| Move the canvas/PDF scale into the data instead of two hard-coded constants | `DEFAULT_SCALE = 1.5` exists independently in both workspaces | |
| Share request/response types between backend and frontend, generated from Zod | The two currently redeclare the contract by hand | |
| An optional name or comment from the respondent | Suggested while reviewing the public form: let whoever fills it in add a name or a note alongside their answers. Deferred deliberately — it is a new column on `Response`, a migration, and **new personal data**, so it needs a row in the data inventory in [07-security](./sot/07-security-and-privacy.md), a length limit, and a decision about whether it appears in the CSV. Worth doing as its own change, not folded into a fix | |
| A local `vuepdf` database from before the baseline is silently a decade behind | The dev database on this machine had **none** of `organizations`, `memberships`, `refresh_tokens` or `invitations`, and `prisma migrate status` reported all seven migrations unapplied including `0_baseline` — so nothing had ever told anyone. Registration 500s, and the E2E suite reports it as an application failure. The recovery is documented in [08-operations](./sot/08-operations.md#database-migrations) (`migrate resolve --applied 0_baseline`, then `migrate deploy`) but nothing points at it when it happens. Hit while building [`features/0011`](../features/0011-adopt-the-design-system.md); `npm run check:node` already exists as the place a check like this would belong | [08-operations](./sot/08-operations.md) |
| The local integration database has to be **created** as well as migrated | The existing row below says nothing migrates `vuepdf_test` locally. It is worse than that: nothing creates it either, and the failure is `Database \`vuepdf_test\` does not exist` across all seven specs. The full local incantation is `docker exec vuepdf-db psql -U postgres -c "CREATE DATABASE vuepdf_test;"` then `DATABASE_URL=…/vuepdf_test npx prisma migrate deploy`. A `pretest:integration` script should do both | [09-quality](./sot/09-quality-and-testing.md) |
| Saving the document orphans the PDF it replaces | `persistEditedDocument` uploads a new file and repoints the form; nothing deletes the previous one. Now that it runs on an explicit save rather than on every placement the trail is much shorter, but it is still unbounded. Wants a form-scoped replace endpoint or a sweep, and it gets more expensive once PDFs live in object storage | |
| Fields cannot be moved or resized while the page is rotated | Drag and resize work in screen deltas, and on a turned page a screen delta is not a stored delta. Rather than write a position the author did not point at, `FormFieldItem.vue` refuses both while `rotation % 360 !== 0` and says so in a tooltip. Mapping the deltas per handle is the real fix | |
| The editor no longer lists open documents or forms | The rail became Fields + Pages, per the `Editor` artboard, which retired the tabbed rail that also held `DocumentsList` and `FormsList`. Forms are reachable from `/dashboard`; the multi-document list is not reachable at all any more. Nothing depended on it in the tests, and whether opening several PDFs at once is a feature worth keeping is an open question rather than an obvious loss | |
| A Spanish toast survives in `FormFieldItem.vue` | `"Error al guardar posición"` on a failed drag-save. Left alone deliberately: the i18n row below says to fix this with a layer, and one more hand-translated string makes that layer harder to justify | |
| Nothing verifies rotation in a browser | The rotation geometry has unit tests and the reactivity faults behind it were found by reading, not by a failing test — a page was rendered at the wrong size and nothing noticed. A test that turns a page and asserts a field's box against the canvas would have caught all four faults; it needs a real render, so Playwright | |
| The editor's drawing tool still does not persist | `useTextPlacement` and `useImagePlacement` now upload the edited PDF; the freehand drawing tool writes to the canvas through a different path and was not covered. Whether it has the same defect is untested — it is the obvious next place to look | |
| i18n layer for UI copy | The visible Spanish strings are gone — the floating toolbar, the field-placement overlay, the search box and the text tool were translated while fixing the editor. What remains is Spanish *comments* and a handful of Spanish toast details deeper in the stack, and the real point stands: the product still hardcodes English and will need a layer rather than another pass of find-and-replace | |
| Server-state library (TanStack Query) for reads | Every store hand-manages loading, error and caching | |
| Evaluate VueUse for `useDragAndDrop`, `useGridOverlay`, `useToolbarDrag` | Case by case; some encode editor-specific behaviour | |
| Soft delete or export prompt before `DELETE /api/forms/:id` | Cascades to every response with no undo | |
| `DELETE /api/forms/:formId/fields/:fieldId` still hard-deletes answers | The bulk save now archives a field that holds responses; the individual delete still cascades them away. Deliberately left as an explicit user act in [`features/0001`](../features/0001-stable-field-ids-and-safe-bulk-save.md), but the two paths should agree — probably by archiving here too, with a confirmation that says how many answers are affected | |
| No E2E coverage of opening a saved form for editing | `handleEdit` in `FormsList.vue` and `FormsManagementView.vue` downloads the form's PDF and loads its fields, and no test touches it. [`features/0006`](../features/0006-signed-expiring-urls-for-uploaded-pdfs.md) changed both and had to be verified by hand against the running app. One Playwright test would cover the whole path | |
| No UI for archived fields | A field archived by a save is invisible to its owner except as a toast at save time and a column in the responses table. No list, no un-archive, no way to tell an archived column from a live one in the dashboard | |
| The bulk save's concurrency guard is untested | The `SELECT … FOR UPDATE` in the bulk handler is what stops a response submitted mid-save from having its answer cascaded away. Nothing exercises it — a real test needs two connections and deliberate interleaving | |
| Nothing exercises a migration against a database that already holds data | CI applies migrations to a fresh database only, so a migration that is fine on empty tables and wrong on populated ones passes | |
| No E2E coverage of hitting a plan limit | The `402` path and the `LimitReached` dialog are covered by unit and integration tests only. A Playwright test would need an account to publish two forms, which is exactly the free plan's limit — cheap to write, and it is the only level that would catch the dialog failing to open. Noticed while building [`features/0012`](../features/0012-plan-catalogue-and-entitlements.md) | |
| A respondent cannot tell an exhausted plan from a closed form, and neither can the author | Deliberate on the public side — see [07-security](./sot/07-security-and-privacy.md). But the **author** gets no signal either: nothing on the dashboard says "this form stopped accepting responses because the month ran out". They find out from the respondent. Needs a state on the form row, and it is the kind of gap that generates support tickets nobody can answer. **Billing sharpened this**: after [`features/0013`](../features/0013-stripe-subscriptions.md) a subscription that finally fails can drop an organization to Free mid-month and take every one of its published forms offline for respondents, still with no signal to the author. Solving it needs a channel this product does not have — there is no email service anywhere in the repo | [`features/0013`](../features/0013-stripe-subscriptions.md) |
| **Remove `DEV_PLAN_KEY`, or replace it with real environments** | It exists because one published form is not a workable development environment, and it is a flag that can switch plan enforcement off. Today it is safe — an allowlist on `NODE_ENV`, pinned off in all three test configurations, and it renames the plan to `Developer` everywhere it is rendered — but the durable answer is a staging deployment that simply runs on a real plan, not a flag anyone has to remember. Delete the block at the bottom of `backend/src/services/plans.ts`, the two `effectivePlan` call sites, and the `.env.example` entry | [08-operations](./sot/08-operations.md) |
| Virus scanning on uploads | Files are stored and served back from our own origin | |
| Real readiness probe | `/health` returns `ok` with the database down | |

## Repository housekeeping

| Item | Status |
|---|---|
| `origin/main` is 17 commits behind `origin/develop` | Every one of [`features/0001`](../features/0001-stable-field-ids-and-safe-bulk-save.md)–[`0006`](../features/0006-signed-expiring-urls-for-uploaded-pdfs.md) lives only on `develop`. That includes the answer-destroying bulk save and all three **High** security findings (S1, S2, S3). The only commits `main` holds and `develop` does not are the merge commits of PR #2 and PR #3. Nothing is deployed from `main` today, so this is not yet an incident — but the moment anything is, `main` is the branch with the known data-loss defect in it. Needs a `develop` → `main` PR, or an explicit decision that `main` is not the release branch |
| Seven merged branches still alive on the remote | `origin/feature/0001`…`0006` are all merged into `develop`, and `origin/feature/sprint-3-public-forms` is merged into `main` via PR #3. Delete them or keep them as history — needs one decision, not seven. The `features/*.md` specs are the durable record of that work, so the branches carry nothing the repository would lose |
| Verify CI is green on the remote | The four suites were confirmed green locally on `develop` at `e0c12bf` on 2026-08-28 — frontend 29 specs/237 tests, backend 10/98, integration 2/14, E2E 6/34, 383 tests in total, on Node 22.22.0 against a real PostgreSQL. What is **not** verified is the GitHub Actions run for the same commit: `gh run list` returns `HTTP 401` for an unauthenticated CLI. A green local run says nothing about the CI environment, which is the whole point of [`features/0005`](../features/0005-working-ci-and-enforced-node-version.md) |
| One unexplained one-off failure in the E2E suite | `auth-flow.spec.ts > should register a new user successfully` failed once during [`features/0007`](../features/0007-security-headers-and-csp.md) and did not recur in five further full runs plus three targeted ones. The failing run took **48.9 s against a usual ~25 s**, which points at machine contention making `waitForURL(/\/dashboard/)` time out rather than at anything in the application — but that is a hypothesis, not a diagnosis. The register limiter is not the cause: `playwright.config.ts` sets `RATE_LIMIT_REGISTER_MAX` to 100000. If it recurs, capture the trace before re-running |
| One unexplained one-off failure in the mocked backend suite | `tests/forms.spec.ts > DELETE /api/forms/:id > should delete form` failed once during `feature/0001` and has not recurred in ~80 full-suite runs since. It mocks only `form.findFirst` and `form.delete`. The two rate-limit flakes that used to share this row were traced during `feature/0003` to assertions depending on limiter hit-counts the test did not control; they now assert the effective limit from the `RateLimit-Policy` header instead, and 50 consecutive full-suite runs are clean. If this one recurs, suspect the same class: a mocked-suite test asserting on state shared through the module-level `app` |
| Two abandoned bulk-fix commits on `develop` (`fb8acd8`, `771b77c`) | The apply-then-revert pair is already on the remote; net effect is zero, left alone rather than rewriting shared history |
