# Node version is a hard requirement, not a suggestion: on an unsupported
# version the frontend suite will not start, the build dies in @vitejs/plugin-vue,
# and the native re2 module silently stops loading. Fail the install instead of
# printing EBADENGINE into a wall of npm output.
engine-strict=true
